import 'package:flutter/material.dart';

class PeopleConnect extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Scaffold(
      appBar: AppBar(
        title: Text('人员工作对接'),
      ),
      body: Text('这里是人员工作对接界面'),
    ));
  }
}
