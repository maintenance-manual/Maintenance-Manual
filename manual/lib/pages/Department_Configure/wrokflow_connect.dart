import 'package:flutter/material.dart';

class WrokflowConfigure extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Scaffold(
      appBar: AppBar(
        title: Text('流程工作对接'),
      ),
      body: Text('这里是流程工作对接界面'),
    ));
  }
}
