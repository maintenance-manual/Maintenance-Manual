import 'package:flutter/material.dart';

class HandBookSearch extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Scaffold(
      appBar: AppBar(
        title: Text('手册查询'),
      ),
      body: Text('这里是手册查询界面'),
    ));
  }
}
